HANDOFF CONTEXT
===============

(Also saved at docs/plans/handoff-2026-09-16.md — generated by /handoff)

USER REQUESTS (AS-IS)
---------------------
- "where was we last time ?"
- "make doc phase 4"
- "start phase 4"
- "ahh i want to ask we are making this web based game right like i wil deploy it later using vercel"
- "ok now make the phase 5 doc"
- "start the phase 5"
- "save the session and chat"

GOAL
----
Run the pending F5 feel/look passes on Phases 4 and 5, then plan and build Phase 6 (race system: countdown, checkpoints, timer, position, finish, restart).

WORK COMPLETED
--------------
- I wrote docs/plans/phase-4-first-person-pov.md (10 decisions, 6 steps), then executed it fully: new camera_fx.gd on the Camera3D node (bob, slope tilt, carve lean, jump pop / landing dip / fall drift via spring-dampers, trauma² shake, FOV kick 78→88, CAM_FX kill switch); controller gained vy_world (fixed the velocity.y≈0 floor-slide quirk), goat_jumped + goat_bonked signals; F3 overlay gained an fx line. Smoke 23/23 PASS.
- I answered the web/Vercel question: project is web-first (gl_compatibility renderer, all-procedural assets); the one Phase 14/15 gotcha is cross-origin isolation headers (COOP/COEP in vercel.json) for SharedArrayBuffer, or a non-threaded export.
- I wrote docs/plans/phase-5-realistic-mountain.md. reffimg/ references were NOT machine-readable that session (model has no image input, multimodal analyzer timed out), so art direction was made explicitly user-gated (D8) with every look number a tunable.
- I executed Phase 5 fully: terrain v2 (1024 m, 260 peak, 192² grid, domain warp, cliff bands with ledge treads, ~210 ms trimesh cook / 73,728 tris — the web load ledger); terrain_blend.gdshader with zone-id stamped by surface_at() (visual snow == grip snow is structural); vegetation.gd (MultiMesh 195 pines / 400 rocks / 1689 tufts, classifier-driven, game-side placement record); atmosphere.gd (altitude fog curve, 8 procedural clouds, 7 distant peaks, animated water plane at 6 m); tumble state (visible Body spins, capsule upright, recovers via STUMBLE) + directional goat_bonked; FOV_BASE single-sourced in Config (D10). Smoke 30/30 PASS including full Phase 2/3/4 regression.
- Wrote phase-4-completion.md and phase-5-completion.md (honest bug logs included).
- The owner committed everything: HEAD is 40356a3 (all of Phase 5 + completion doc); working tree is CLEAN.

CURRENT STATE
-------------
- Godot 4.7.2 project at VERSION 0.5.0-phase5; godot binary at /opt/homebrew/bin/godot; renderer gl_compatibility (web requirement).
- Last verification: godot --headless --import → 0 errors; --quit-after 60 → 0 errors; phase-5 smoke 30/30 PASS then deleted (temp pattern).
- Git: clean tree, everything committed by the owner through 40356a3.
- Terrain API is FROZEN and regression-proven: get_height_at / surface_at / slope_deg_at / downhill_dir. Phase 6 must not change signatures.

PENDING TASKS
-------------
- USER-SIDE (the real gates, cannot be done headless):
  - Phase 4 F5 feel pass (checklist in phase-4-completion.md) + gate question: "animal or shopping cart?"
  - Phase 5 F5 look pass AGAINST reffimg/ (checklist in phase-5-completion.md) + gate question: "place, or level?" + 10-min nausea/60-FPS-feel check on desktop Chrome
- Next build phase: Phase 6 — race system (make the doc first, matching the phase-plan format, then execute).
- Carried notes for Phase 6 (from phase-5-completion.md): downhill_dir returns ZERO on perfectly flat band treads (checkpoint orientation needs a fallback); Vegetation.get_placements() exists so the race line can route around pines; EventBus.race_started/race_finished signals have existed since Phase 1; distant-peak ring marks the world boundary.
- Later: real goat model / skeletal ragdoll (tumble is the placeholder); water is visual-only; vegetation has no collision until Phase 8; web export + Vercel headers at Phases 14–15.

KEY FILES
---------
- docs/plans/phase-5-completion.md - authoritative record of Phase 5: decisions, bugs, open user items, Phase 6 handoff notes
- docs/plans/phase-6 (not yet written) - next deliverable; follow docs/plans/phase-5-realistic-mountain.md format
- scripts/player/goat_controller.gd - movement + TUMBLE; state machine GROUND/AIR/SLIDE/STUMBLE/TUMBLE; get_debug_state() is the shared readout
- scripts/player/camera_fx.gd - all camera FX; additive local offsets ONLY on Camera3D (never Head — head_camera.gd writes rotation absolutely); CAM_FX kill switch
- scripts/terrain/terrain_generator.gd - v2 terrain; look tunables are @exports; API frozen
- scripts/terrain/vegetation.gd - MultiMesh scatter + get_placements(kind) game-side record
- scripts/terrain/atmosphere.gd - fog curve/clouds/peaks/water
- scripts/game/config.gd - every tunable, VERSION 0.5.0-phase5
- README.md - the 17-phase master plan; MVP = one mountain + goat + horn POV + downhill physics + finish line

IMPORTANT DECISIONS
-------------------
- Camera effects live on Camera3D, never Head (absolute rotation writes would wipe them); horns stay children of Camera3D so they inherit offsets (steady framing).
- Visual zones must equal grip zones: the terrain shader shades WITHIN zones stamped by surface_at(); it never invents boundaries.
- Vegetation placement derives from the same surface classifier as grip; caps in Config are ceilings for web perf.
- Tumble = ragdoll-lite: capsule stays upright, only visible Body spins; recovery hands off to the verified STUMBLE beat.
- --script smoke pattern (CRITICAL for future headless tests): entry scripts compile BEFORE autoload globals register — no autoload identifiers, no game class_names, no constant-string load() paths; fetch Config/EventBus via root.get_node("/root/...") at runtime. ALSO: the dummy RenderingServer drops MultiMesh instance data headless — audit placements via game-side records, not get_instance_transform.
- Phase rhythm established: plan doc (decisions table + steps + commit checkpoints + user F5 checklist) → execute with per-step verification → temp smoke script (deleted after) → completion doc (honest bug log) → owner commits.

EXPLICIT CONSTRAINTS
--------------------
From ~/.config/opencode/AGENTS.md (owner directive 2026-08-24), verbatim core: "I must NEVER, under ANY circumstance: Run git commit ... Run git push ... Run ANY git write command: git add, git stash, git reset, git rebase, git cherry-pick, git revert, git merge, git restore, git rm, git mv, git tag, git branch, git checkout -b, git clean ... The OWNER does 100% of commits, pushes, and git history operations. Always." — I report changed files + suggested commit messages, then STOP. Read-only git (status/diff/log/show/grep/blame/ls-files) is allowed.

CONTEXT FOR CONTINUATION
------------------------
- The user is the owner/commiter; they commit in bursts while or after I work — check git status/log at session start to resync.
- Phase 4 tilt/lean signs and ALL Phase 5 look numbers are first guesses derived headless; F5 passes are the real gates. First lever for camera nausea: halve amplitudes.
- Headless cannot verify visuals or FPS — structure asserts + user eyeball is the established division of labor.
- reffimg/ contains the user's alpine reference images (4 jpegs); the Phase 5 plan could not read them — if a future session has image input, reviewing them against the terrain tunables is high-value.
- The web target (desktop Chrome 60 FPS, Vercel later) shapes perf decisions: MultiMesh, shadow caps, fog-as-culling, no post-FX, load-time ledger (~210 ms terrain cook is the first data point).
