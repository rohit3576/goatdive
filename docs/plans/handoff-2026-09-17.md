HANDOFF CONTEXT
===============

(Also saved at docs/plans/handoff-2026-09-17.md — same pattern as
handoff-2026-09-16.md, generated at the user's "save the chat" request.)

USER REQUESTS (AS-IS)
---------------------
- "where was we left ?"
- "ok make the doc of phase 6 and yes I want to check for real how is it working now"
- "ok now start with the phase 6"
- "now creae the next phase doc"
- "ok start with the phase 7"
- "why it is takkig time ?"
- "make doc for next phase"
- "save the chat"

GOAL
----
Phases 6 and 7 executed end-to-end (race system + AI goat herd); Phase 8
plan doc written. Next: user F5 verdicts on 6+7, then execute Phase 8
(mountain gameplay: corridor colliders, logs, danger chords).

WORK COMPLETED
--------------
- Wrote + executed docs/plans/phase-6-race-system.md fully: course_builder.gd
  (downhill walk, 7 gates, 512 m, pine avoidance), checkpoint.gd (Area3D
  gates), race_manager.gd (countdown/timer/order/gate-respawn/racer-list),
  race_hud.gd (countdown/top bar/results), F3 race line, gate-pass FOV pop.
  Smoke 34/34 PASS incl. remount restart + Phase 2-5 regression. Owner
  committed as 1ba861d.
- Wrote + executed docs/plans/phase-7-ai-goats.md fully: ai_goat.gd brain
  (waypoint lookahead, body-yaw ownership, speed caps, ledge jumps, dual
  rescue nets), controller autopilot (set_autopilot/set_ai_steering),
  spawn-grid herd with tinted bodies + stripped player rigs, multi-racer
  manager (per-racer gates/records/respawn/finish times, standings, one
  clock until all finish), HUD standings, F3 AI lines. Grand prix smoke
  29/29 PASS: RECKLESS 0:32.25 / YOU 0:35.53 / CAUTIOUS 0:37.28 / BOLD
  (KILL_Y survivor) — positions live 1-4, regression green. Owner
  committed as 0c5842e.
- Wrote docs/plans/phase-8-mountain-gameplay.md (corridor colliders on
  vegetation records, AI record-based avoidance, fallen logs, generated
  danger-chord shortcuts, herd-as-difficulty-telemetry, 10 decisions,
  ~7.5 h). UNTRACKED — awaiting owner commit.
- Wrote phase-6-completion.md and phase-7-completion.md (honest bug logs).

CURRENT STATE
-------------
- Godot 4.7.2, VERSION 0.7.0-phase7, renderer gl_compatibility.
- HEAD 0c5842e (all of Phase 7 committed). Untracked: the Phase 8 plan doc
  (+ this handoff once written). Working tree otherwise CLEAN.
- Last verification: import + 400-frame headless run = 0 errors; course
  7 gates / 512 m / ~2 ms. Game window was relaunched for the user with
  the Phase 7 build (herd racing).
- Terrain API still FROZEN and regression-proven. Race system: gates are
  polyline vertices; manager owns order per-racer; brains own body yaw.

PENDING TASKS
------------
- USER-SIDE F5 gates (the real product gates, cannot be done headless):
  - Phase 6 checklist (phase-6-completion.md): gate line readable at
    speed, gate-respawn = relief, wrong-way flash keep/cut, "loop or tech
    demo?"
  - Phase 7 checklist (phase-7-completion.md): "herd or ghosts?", rescue
    teleport visibility (AI_GATE_STALL tunable), bump fairness (one-line
    layer fallback exists if pinball), RECKLESS coin-flip check, 60 FPS
    with 4 goats
- Phase 8 execution (doc ready, decisions locked): corridor colliders →
  brain avoidance → fallen logs → danger chords → telemetry → gauntlet
  smoke. Step 2's done-condition is quantitative (-70% AI hits, <10%
  pace drift).
- Owner commits pending: "doc: phase 8 mountain gameplay execution plan"
  (and this handoff doc if wanted).
- Carried: real goat model / skeletal ragdoll; water visual-only; AI
  "later list" (personalities, shortcuts, difficulty); finish-altitude
  tuning; reffimg/ jpegs still untracked since Phase 1.

KEY FILES
---------
- docs/plans/phase-8-mountain-gameplay.md - next deliverable's plan (locked)
- docs/plans/phase-7-completion.md / phase-6-completion.md - authoritative
  records incl. Phase 8 handoff notes
- scripts/race/course_builder.gd - generated course + polyline (D5 math)
- scripts/race/ai_goat.gd - the brain (lookahead, yaw, caps, rescues)
- scripts/race/race_manager.gd - multi-racer state machine, standings
- scripts/race/checkpoint.gd - gates: visual state + set_armed split
- scripts/race/race_hud.gd / scripts/utils/debug_overlay.gd - HUD + F3
- scripts/player/goat_controller.gd - movement + autopilot + input lock
- scripts/game/config.gd - every tunable, VERSION 0.7.0-phase7
- scripts/terrain/vegetation.gd - MultiMesh + get_placements() records
  (Phase 8's collider substrate)

IMPORTANT DECISIONS + LESSONS (this session's additions)
--------------------------------------------------------
- Gate triggers are CROSSING SLABS (25 m deep, +14 m side margin), not
  thin boxes: 3x14 m boxes gave 0/7 passes for goats running the whole
  course. Generous triggers, honest obstacles (D2 Phase 8) — the pair.
- Order enforcement + forward-only brains = skip-gate deadlock; the
  gate-stall rescue (12 s without a gate progress -> teleport before
  current gate) is the guarantee of finite races.
- The brain OWNS body yaw (the player's mouse yaws the goat BODY via
  head_camera) — AI goats get their player rig stripped (overlay freed,
  head input off, camera not current).
- Area3D.monitoring defaults TRUE at birth; set_deferred for arming
  changes (physics blocks direct writes inside body_entered).
- --script smoke harness rules (now battle-tested across two phases):
  single mount point; waits live on ACTION steps (violated twice —
  hard rule); engine pre-increments step index (insert at _step_i);
  wall-clock timing ONLY (headless fps varies 100-200; frame math lies);
  duck-called game methods get explicit types (one bad := inference
  cascaded into 11 errors); GDScript.new() needs reload();
  SceneTreeTimer needs a _process override; SceneTree is not a Node.
- Physics DOES tick in --script mode (goats fall/settle/jump/race) —
  proven; only RenderingServer-backed things (MultiMesh readback) lie.
- Ternary-statement side effects and empty-dict-vs-null checks: GDScript
  footguns logged in the completion docs.

EXPLICIT CONSTRAINTS
--------------------
From ~/.config/opencode/AGENTS.md (owner directive 2026-08-24), verbatim
core: "I must NEVER, under ANY circumstance: Run git commit ... Run git
push ... Run ANY git write command: git add, git stash, git reset, git
rebase, git cherry-pick, git revert, git merge, git restore, git rm, git
mv, git tag, git branch, git checkout -b, git clean ... The OWNER does
100% of commits, pushes, and git history operations. Always." — I report
changed files + suggested commit messages, then STOP. Read-only git
(status/diff/log/show/grep/blame/ls-files) is allowed.

CONTEXT FOR CONTINUATION
------------------------
- The user is the owner/committer; they committed 6 and 7 promptly this
  session. Check git status/log at session start to resync.
- Both F5 gates are still open — the user launched the game twice but has
  given no verdicts yet. All feel/look numbers remain tunables; first
  camera lever: halve amplitudes; first rescue-visibility lever:
  AI_GATE_STALL up.
- Phase rhythm (established and now twice-proven at scale): plan doc
  (decisions + steps + commit checkpoints + F5 checklist) -> execute with
  per-step verification -> temp smoke (deleted after) -> completion doc
  (honest bug log) -> owner commits.
- Web target (desktop Chrome 60 FPS, Vercel at 14-15) keeps shaping
  decisions: MultiMesh, one-body-many-shapes colliders planned for
  Phase 8, load ledger habit (terrain ~205 ms / course ~2 ms / vegetation
  ~38 ms are the current numbers).
- The herd doubles as infrastructure: crash-test dummies grade Phase 8's
  obstacle difficulty (gauntlet harness pattern in the Phase 7 smoke).
